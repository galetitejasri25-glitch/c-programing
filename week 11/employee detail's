#include <stdio.h>
struct emp {
    char name[20];
    int id;
};
int main() {
    struct emp e[3];
    for(int i=0;i<3;i++)
        scanf("%s %d", e[i].name, &e[i].id);
    for(int i=0;i<3;i++)
        printf("%s %d\n", e[i].name, e[i].id);
    return 0;
}

#include <stdio.h>
#include <string.h>
int main() {
    FILE *fp;
    char str[100];
    fp = fopen("a.txt", "r");
    fgets(str, 100, fp);
    fclose(fp);
    for(int i=strlen(str)-1;i>=0;i--)
        printf("%c", str[i]);
    return 0;
}
